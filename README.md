# DataNest

A static DataNest demo site deployed to GitHub.

## Usage

- Open `index.html` to load the app.
- Upload an `.xlsx`, `.xls`, or `.csv` file to preview records.

## GitHub Pages

To publish this site on the internet, enable GitHub Pages in the repository settings and use:

`https://gkdworkcyber.github.io/datanest/`
